# Boots a real Home Assistant, so it needs Python 3.14.2+ (HA 2026.7).
# The tests under tests/unit need only aiohttp and pytest.
pytest-homeassistant-custom-component

# Requirements of the Home Assistant integrations this component builds on.
# A custom component does not inherit them, so they are listed explicitly:
#   tts    -> mutagen        (homeassistant/components/tts/manifest.json)
#   ffmpeg -> ha-ffmpeg      (a dependency of tts)
mutagen==1.47.0
ha-ffmpeg==3.2.2
